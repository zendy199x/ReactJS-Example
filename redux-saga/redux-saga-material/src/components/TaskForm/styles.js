const styles = theme => ({
  container: {
    display: 'flex',
    flexWrap: 'wrap'
  },
  dialogContent: {
    display: 'flex',
    flexDirection: 'column'
  },
  textField: {
    minWidth: 500,
    width: '100%'
  }
});

export default styles;
