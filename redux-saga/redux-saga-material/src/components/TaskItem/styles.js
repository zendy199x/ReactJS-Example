const styles = theme => ({
  cardActions: {
    alignItems: 'center',
    justifyContent: 'flex-end'
  },
  labelStatus: {
    textAlign: 'right'
  },
  card: {
    marginTop: theme.spacing(2)
  }
});

export default styles;
